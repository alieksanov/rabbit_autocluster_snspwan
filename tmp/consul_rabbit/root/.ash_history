ll
ls -la
ls -la /
ls -la /consul
consul
ip a
consul agent -dev
consul agent -dev 0.0.0.0
consul agent -dev -client 0.0.0.0
ls -la /etc/conf.d/
consul agent -config-dir /consul/config
consul agent -dev -client 0.0.0.0
consul agent -config-dir /consul/config
consul agent -config-dir /consul/config 
consul agent -config-dir /consul/config -ui
which consul
consul agent -config-dir /consul/config -ui
which consul
ps xa
consul agent -config-dir /consul/config -ui
